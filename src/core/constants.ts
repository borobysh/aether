export const DEFAULT_CELL_SIZE = 512;
export const DEFAULT_PICK_RADIUS = 5;
export const DEFAULT_MIN_ZOOM = 0.1;
export const DEFAULT_MAX_ZOOM = 10;
export const DEFAULT_ZOOM_SPEED = 1.1;
export const DEFAULT_FIT_BOUNDS_PADDING = 0.9;

export const MOUSE_BUTTON = {
    LEFT: 0,
    MIDDLE: 1,
    RIGHT: 2
} as const;

export const HOVER_THROTTLE_MS = 16;
export const DDA_SAFETY_LIMIT = 10000;

